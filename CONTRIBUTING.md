# Contributing

Thank you for helping make pull request review more manageable.

1. Open an issue before large behavior changes.
2. Keep pull requests focused and include tests.
3. Run `python -m unittest discover -s tests -v`.
4. Explain user-visible changes in the pull request description.

Changes should preserve zero runtime dependencies unless there is a strong,
documented reason otherwise.
